{
  "name": "@features/transactions",
  "version": "1.3.0",
  "private": true,
  "description": "Transactions feature slice (server) for gastos-personales-reference. Slice 5 ships the type layer (T5.1 schema, T5.4 schemas, T5.5 entities, T5.6 ports). Behavior lands in slice 5 PR #3.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit",
    "lint": "eslint . --max-warnings 0",
    "bdd": "cd ../docs && NODE_OPTIONS='--import tsx/cjs' cucumber-js --config cucumber.mjs"
  },
  "dependencies": {
    "@core/config": "workspace:*",
    "@core/database": "workspace:*",
    "@core/events": "workspace:*",
    "@shared-utils/decimal": "workspace:*",
    "zod": "4.4.3"
  },
  "devDependencies": {
    "@types/node": "22.18.0",
    "typescript": "6.0.3",
    "vitest": "4.1.9",
    "eslint": "^10.6.0",
    "zod": "4.4.3"
  }
}
