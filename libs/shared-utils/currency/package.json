{
  "name": "@shared-utils/currency",
  "version": "1.1.0",
  "private": true,
  "description": "Format decimal.js values to localized currency strings via Intl.NumberFormat. Pure helpers, no I/O.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit",
    "lint": "eslint . --max-warnings 0"
  },
  "dependencies": {
    "decimal.js": "10.6.0"
  },
  "devDependencies": {
    "@types/node": "22.18.0",
    "vitest": "4.1.9",
    "typescript": "6.0.3"
  }
}