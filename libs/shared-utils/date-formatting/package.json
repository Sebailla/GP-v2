{
  "name": "@shared-utils/date-formatting",
  "version": "1.2.0",
  "private": true,
  "description": "Timezone-safe date formatting (Intl.DateTimeFormat) + ISO 8601 parsing for gastos-personales-reference. Pure helpers, no I/O.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit",
    "lint": "eslint . --max-warnings 0"
  },
  "devDependencies": {
    "@types/node": "22.18.0",
    "vitest": "4.1.9",
    "typescript": "6.0.3",
    "eslint": "^10.6.0"
  }
}
