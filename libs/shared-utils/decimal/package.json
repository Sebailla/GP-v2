{
  "name": "@shared-utils/decimal",
  "version": "0.0.0",
  "private": true,
  "description": "Wrappers around decimal.js for monetary math. NEVER BigInt (per D-TX-6). Pure helpers, no I/O.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit",
    "lint": "eslint . --max-warnings 0"
  },
  "dependencies": {
    "decimal.js": "10.6.0"
  },
  "devDependencies": {
    "@types/node": "22.18.0",
    "vitest": "4.1.9",
    "typescript": "6.0.3"
  }
}